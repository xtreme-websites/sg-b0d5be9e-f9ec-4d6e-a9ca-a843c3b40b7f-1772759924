import type { OpenWebNinjaRequest, OpenWebNinjaResponse } from "@/types";

/**
 * Service for triggering rank checks via the OpenWeb Ninja
... + ":\n" + errorData.error);
      }
      throw new Error("Failed to generate AI insight");
    }
  }
};